#include<bits/stdc++.h>

using namespace std;
const int N = 1e5 + 5;


void solve(int cs) {
  cout << "Case #" << cs << ": ";
}

int main() {
  ios_base::sync_with_stdio(0);

  int TT = 1;
  cin >> TT;
  for(int tc = 1; tc <= TT; ++i) {
    solve(tc);
  }

  return 0;
}
